plugins {
    id "com.diffplug.gradle.spotless" version "3.15.0"
}

apply plugin: 'java'

spotless {
    java {
        googleJavaFormat()
        target '**/*.java'
    }
}

// TODO just to prevent error when using old command to deploy.Will remove it later.
task generate {

}